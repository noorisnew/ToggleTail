/**
 * Story Library Seeder (Prisma version)
 *
 * Delegates to the canonical Prisma seed file.
 * Usage: node src/scripts/seedLibrary.js
 *        — or —  npm run db:seed  (from backend/)
 */

require('../../prisma/seed');
